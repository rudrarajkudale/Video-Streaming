<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Video</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #121212;
            color: #fff;
            text-align: center;
        }

        h1 {
            margin-top: 20px;
        }

        .container {
            width: 50%;
            margin: auto;
            background: #1e1e1e;
            padding: 20px;
            border-radius: 10px;
        }

        video {
            width: 100%;
            border-radius: 8px;
        }

        .btn {
            padding: 10px 20px;
            margin-top: 10px;
            border-radius: 5px;
            font-weight: bold;
            text-decoration: none;
            cursor: pointer;
            border: none;
        }

        .save-btn {
            background: #4CAF50;
            color: #fff;
        }

        .cancel-btn {
            background: #f44336;
            color: #fff;
        }
    </style>
</head>
<body>
    <h1>Edit Video</h1>

    <div class="container">
        <video controls>
            <source src="<%= video.url %>" type="video/mp4">
        </video>
        <form action="/update/<%= video._id %>" method="POST">
            <label for="name">Change Video Name:</label>
            <input type="text" name="name" value="<%= video.name %>" required>
            <br>
            <button type="submit" class="btn save-btn">Save Changes</button>
            <a href="/show" class="btn cancel-btn">Cancel</a>
        </form>
    </div>
</body>
</html>
