<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Video</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #121212;
            color: #fff;
            text-align: center;
            margin: 0;
            padding: 0;
        }

        h1 {
            margin-top: 20px;
            font-size: 2rem;
        }

        .container {
            width: 60%;
            margin: 30px auto;
            padding: 20px;
            background: #1e1e1e;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(255, 255, 255, 0.2);
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        label {
            font-size: 1.2rem;
            margin-bottom: 10px;
        }

        input[type="file"] {
            margin: 10px 0;
            padding: 10px;
            background: #272727;
            color: #fff;
            border: none;
            border-radius: 5px;
        }

        button {
            padding: 10px 20px;
            background: #ff9800;
            color: #fff;
            font-size: 1rem;
            font-weight: bold;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: 0.3s;
        }

        button:hover {
            background: #e68900;
        }

        .success-message {
            margin-top: 20px;
            padding: 15px;
            background: #2e7d32;
            color: #fff;
            border-radius: 5px;
        }

        a button {
            margin-top: 15px;
            background: #03a9f4;
        }

        a button:hover {
            background: #0288d1;
        }
    </style>
</head>
<body>
    <h1>Upload Your Video</h1>
    <div class="container">
        <form action="/upload" method="POST" enctype="multipart/form-data">
            <label for="video">Choose Video to Upload:</label>
            <input type="file" name="video" accept="video/*" required>
            <button type="submit">Upload</button>
        </form>

        <% if (typeof fileUrl !== 'undefined') { %>
            <div class="success-message">
                <h2>Video Uploaded Successfully!</h2>
                <p>File URL: <a href="<%= fileUrl %>" target="_blank" style="color: #ff9800;"><%= fileUrl %></a></p>
            </div>
        <% } %>

        <a href="/show">
            <button type="button">📺 View All Uploaded Videos</button>
        </a>
    </div>
</body>
</html>
